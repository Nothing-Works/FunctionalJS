// declare a variable called a and set it to 10.
let a = 10;
// declare a variable called b and set it to a. so b is 10.
let b = a;
// set a to 0.
a = 0;
//so a is 0, b is 10.
console.log(a); //0;
console.log(b); //10;